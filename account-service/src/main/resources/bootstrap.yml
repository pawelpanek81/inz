server:
  port: 8083

spring:
  application:
    name: map-service
#  cloud:
#    config:
#      uri: http://config-server:9090
#      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:9091/eureka