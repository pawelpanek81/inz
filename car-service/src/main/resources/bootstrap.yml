server:
  port: 8086

spring:
  application:
    name: car-service
  cloud:
    config:
      uri: http://config-server:9090
      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:9091/eureka