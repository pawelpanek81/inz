server:
  port: 9090

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          searchLocations:
            - classpath:/cfg/
  profiles:
    active: native