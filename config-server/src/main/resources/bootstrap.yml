server:
  port: 9090

spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        native:
          searchLocations:
            - classpath:/config
  profiles:
    active: native